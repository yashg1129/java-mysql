server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  datasource:
    url: jdbc:mysql://3.90.16.209:3306/appdb?useSSL=false&serverTimezone=UTC
    username: ravindra
    password: Yashvi@712
    driver-class-name: com.mysql.cj.jdbc.Driver